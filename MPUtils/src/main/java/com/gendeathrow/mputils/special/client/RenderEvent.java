package com.gendeathrow.mputils.special.client;

import net.minecraftforge.client.event.RenderPlayerEvent;
import net.minecraftforge.fml.common.eventhandler.SubscribeEvent;

public class RenderEvent 
{

	
	@SubscribeEvent
	public void renderPlayers(RenderPlayerEvent event)
	{
		
		//event.getRenderer().getMainModel().copyModelAngles(source, dest);
		
	}
}
